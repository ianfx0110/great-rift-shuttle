<nav class="bg-card/80 backdrop-blur-md border-b border-border fixed w-full z-50">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between h-16 items-center">
      <div class="flex items-center space-x-2">
        <a href="/" class="flex items-center space-x-3 group">
          <div class="bg-primary p-2 rounded-xl shadow-lg group-hover:bg-blue-800 transition-colors">
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" class="text-white w-6 h-6">
              <path d="M17.8 19.2 16 11l3.5-3.5C21 6 21.5 4 21 3c-1-.5-3 0-4.5 1.5L13 8l-8.2-1.8c-1.2-.3-2.4.4-2.7 1.6-.2.6 0 1.2.3 1.6L8 12l-4 4v2l3-1 3 3h2l4-4 2.6 5.7c.4 1 1.6 1.4 2.6 1s1.4-1.6 1-2.6L17.8 19.2Z"/>
            </svg>
          </div>
          <div class="flex flex-col leading-none">
            <span class="text-lg font-black tracking-tighter text-primary uppercase italic">Great Rift</span>
            <span class="text-[10px] font-black tracking-[0.25em] text-accent uppercase">Shuttle Sacco</span>
          </div>
        </a>
      </div>
      
      <!-- Desktop Menu -->
      <div class="hidden md:flex space-x-6 items-center font-bold text-gray-500 text-sm">
        <a href="/" class="nav-link">Home</a>
        <a href="/booking" class="nav-link">Book Trip</a>
        <a href="/routes" class="nav-link">Routes</a>
        <a href="/parcel" class="nav-link">Parcels</a>
        <a href="/tracking" class="nav-link">Track Cargo</a>
        <a href="/contact" class="nav-link">Support</a>
        
        <button id="theme-toggle" class="p-2 rounded-lg bg-bg border border-border text-primary hover:text-accent transition-colors">
          <i data-lucide="sun" id="sun-icon" class="w-4 h-4 hidden"></i>
          <i data-lucide="moon" id="moon-icon" class="w-4 h-4 hidden"></i>
        </button>

        <a href="/login" id="nav-login" class="bg-primary text-white px-5 py-2 rounded-lg hover:bg-blue-900 transition-all shadow-sm">Login</a>
        <div id="user-menu" class="hidden flex items-center space-x-4 border-l border-border pl-6">
          <a href="/dashboard" class="nav-link text-accent">Account</a>
          <button id="logout-btn" class="text-red-500 hover:text-red-700 text-xs font-black uppercase tracking-widest">Logout</button>
        </div>
      </div>

      <!-- Mobile Menu Button -->
      <div class="md:hidden flex items-center space-x-4">
        <button id="mobile-theme-toggle" class="p-2 text-primary">
          <i data-lucide="moon" class="w-5 h-5"></i>
        </button>
        <button id="mobile-menu-open" class="p-2 text-primary">
          <i data-lucide="menu" class="w-6 h-6"></i>
        </button>
      </div>
    </div>
  </div>

  <!-- Mobile Menu Overlay -->
  <div id="mobile-backdrop" class="fixed inset-0 bg-black/50 backdrop-blur-sm z-[55] hidden transition-opacity duration-300"></div>
  <div id="mobile-menu" class="fixed inset-0 bg-card z-[60] transform translate-x-full transition-transform duration-300 ease-in-out md:hidden">
    <div class="flex flex-col h-full">
      <div class="flex justify-between items-center h-16 px-6 border-b border-border">
        <span class="text-lg font-black text-primary uppercase italic">Hub Terminal</span>
        <button id="mobile-menu-close" class="p-2 text-primary hover:text-accent transition-colors">
          <i data-lucide="x" class="w-7 h-7"></i>
        </button>
      </div>
      <div class="flex-1 overflow-y-auto py-8 px-6 space-y-8">
        <a href="/" class="block text-3xl font-black text-primary uppercase tracking-tighter italic hover:text-accent transition-all">Home</a>
        <a href="/booking" class="block text-3xl font-black text-primary uppercase tracking-tighter italic hover:text-accent transition-all">Book Trip</a>
        <a href="/routes" class="block text-3xl font-black text-primary uppercase tracking-tighter italic hover:text-accent transition-all">Routes</a>
        <a href="/parcel" class="block text-3xl font-black text-primary uppercase tracking-tighter italic hover:text-accent transition-all">Parcels</a>
        <a href="/tracking" class="block text-3xl font-black text-primary uppercase tracking-tighter italic hover:text-accent transition-all">Track Cargo</a>
        <a href="/contact" class="block text-3xl font-black text-primary uppercase tracking-tighter italic hover:text-accent transition-all border-b-2 border-accent pb-2">Support</a>
        <hr class="border-border"/>
        <div id="mobile-auth-section">
          <a href="/login" class="block w-full bg-primary text-white text-center py-5 rounded-2xl font-black uppercase tracking-[0.2em] text-xs shadow-xl shadow-primary/20">Login / Register</a>
        </div>
        <div id="mobile-user-section" class="hidden space-y-8">
           <a href="/dashboard" class="block text-3xl font-black text-accent uppercase tracking-tighter italic">My Account</a>
           <button id="mobile-logout-btn" class="block w-full bg-red-50 text-red-600 py-5 rounded-2xl font-black uppercase tracking-widest text-xs">Sign Out</button>
        </div>
      </div>
      <div class="p-6 border-t border-border bg-bg">
         <p class="text-[10px] font-black text-gray-400 uppercase tracking-widest mb-1">&copy; 2026 Great Rift Shuttle</p>
         <p class="text-[8px] font-bold text-gray-400">Digitizing the Rift corridor.</p>
      </div>
    </div>
  </div>
</nav>
<div class="h-16"></div> <!-- Spacer for fixed nav -->

<script>
  function setupNavigation() {
    const mobileMenuOpen = document.getElementById('mobile-menu-open');
    const mobileMenuClose = document.getElementById('mobile-menu-close');
    const mobileMenu = document.getElementById('mobile-menu');
    const mobileBackdrop = document.getElementById('mobile-backdrop');
    
    function toggleMenu(isOpen) {
      if (isOpen) {
        mobileMenu.classList.remove('translate-x-full');
        mobileBackdrop.classList.remove('hidden');
        document.body.style.overflow = 'hidden';
      } else {
        mobileMenu.classList.add('translate-x-full');
        mobileBackdrop.classList.add('hidden');
        document.body.style.overflow = '';
      }
    }

    mobileMenuOpen.onclick = () => toggleMenu(true);
    mobileMenuClose.onclick = () => toggleMenu(false);
    mobileBackdrop.onclick = () => toggleMenu(false);

    // Close on link click
    mobileMenu.querySelectorAll('a').forEach(link => {
      link.onclick = () => toggleMenu(false);
    });

    // Theme support
    const sunIcon = document.getElementById('sun-icon');
    const moonIcon = document.getElementById('moon-icon');
    const themeToggle = document.getElementById('theme-toggle');
    const mobileThemeToggle = document.getElementById('mobile-theme-toggle');

    function updateIcons(isDark) {
      if (sunIcon && moonIcon) {
        if (isDark) {
          sunIcon.classList.remove('hidden');
          moonIcon.classList.add('hidden');
        } else {
          sunIcon.classList.add('hidden');
          moonIcon.classList.remove('hidden');
        }
      }
      if (mobileThemeToggle) {
        mobileThemeToggle.innerHTML = isDark ? '<i data-lucide="sun" class="w-5 h-5"></i>' : '<i data-lucide="moon" class="w-5 h-5"></i>';
        lucide.createIcons();
      }
    }

    const isDark = document.documentElement.classList.contains('dark');
    updateIcons(isDark);

    const toggle = () => {
      const isDark = document.documentElement.classList.toggle('dark');
      localStorage.setItem('theme', isDark ? 'dark' : 'light');
      updateIcons(isDark);
      lucide.createIcons();
    };

    if (themeToggle) themeToggle.onclick = toggle;
    if (mobileThemeToggle) mobileThemeToggle.onclick = toggle;
  }

  // Run on load
  document.addEventListener('DOMContentLoaded', setupNavigation);
</script>
